# python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# the raw dataset is large (2.7 GB) so it is not pushed.
# only the explanatory note inside data/ is tracked.
data/*
!data/DATA_EXPLAINED.md

# the preprocessing cache is regenerated by src/phase1_preprocess.py
outputs/cache/

# my own private notes (not for the company)
docs-for-me/
